# Single project-local config file — contains credentials and host-specific settings
local.env

# Generated by `make configure-k3d`
config/k3d-cluster.yaml

# Downloaded chart tarballs, extracted charts, and per-version values (charts/<version>/)
charts/
